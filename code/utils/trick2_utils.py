import numpy as np 
def take_timing():
    """this function should be used to run on the video 
        and should return the timing start and end corresponding 
        to the two time the magician touch the bottle with the wand
    """
    start = 0 
    end = 0
    return start , end 

def switch_event()
    """This function should be used as preprocessing and should return 
        switch1_event which correspond to the frames index at which the wand hit the ball
        switch2_event same but for the bottle 
        switch3_event same but for the third object 
    """
    frame_idx = np.zeros(7) # 3 ball + 2 bottle + 2 third object
    return frame_idx

