import numpy as np

grp = 11

FIXED = False
DEBUG = True

if FIXED:
    t = "fixed"
else:
    t = "dynamic"

IN_PATH = f"input/video_group_{grp}_{t}.mp4"
JSON_PATH = f"input/annotations_group_{grp}.json"
FILE_NAME = f"group_{grp}_{t}"

OUT_PATH = f"output/{FILE_NAME}.mp4"

N_OBJECT = 3
# 0 : ball || 1 : bottle || 2 : obj3

# -------------- YOLO -------------- #

MODEL_PATH = "model/yolov8n.pt"
READ_EVERY_X_FRAME = 1
CONF_TRESHOLD = 0.0

MAX_W_OBJ = 75
MAX_H_OBJ = 175

ALPHA_CIOU = None

CONTROL_LIST = []

BLACKLIST = ['person', 'skateboard', 'laptop', 'cup', 'chair', 'dining table', 'microwave', 'umbrella', 
    'kite', 'cat', 'traffic light', 'book', 'cell phone', 'keyboard', 'scissors', 'frisbee', 'suitcase', 'dog', 'tv', "handbag"]

# -------------- TRACKER -------------- #

# -- Timing -- #

OBJ_TRACKER_START_FRAME = 420 # (14s)
OBJ_TRACKER_END_FRAME = 2160 # (72s)

OBJ_BALL_TRACKER_START_FRAME = 2340 # (78s)
OBJ_BALL_TRACKER_END_FRAME = None

WAND_2_TRACKER_START_FRAME = 1500 # (50s)
WAND_2_TRACKER_END_FRAME = 2160 # (72s)

WAND_3_TRACKER_START_FRAME = 2610 # (87s)
WAND_3_TRACKER_END_FRAME = 2880 # (96s)

# -- Kalman filter -- #

KALMAN_OBJ_TIMER = 10
KF_OBJ_PROCESS_NOISE = 0.001
KF_OBJ_MEASUREMENT_NOISE = 0.05

KALMAN_WAND_TIMER = 10
WAND_MAX_PRED_JUMP = 10
KF_WAND_PROCESS_NOISE = 0.01
KF_WAND_MEASUREMENT_NOISE = 0.05

# -- red -- #

LOWER_RED1 = np.array([170, 90, 50])
UPPER_RED1 = np.array([180, 255, 255])

LOWER_RED2 = np.array([170, 90, 50])
UPPER_RED2 = np.array([180, 255, 255])

RED_MEDIAN_BLUR = 5

# -- green -- #

GREEN = [0,255,0]
LOWER_GREEN = [40,40]
UPPER_GREEN = [255,255]
TUNNING_GREEN = 35

# -- remove object -- #

SCALE_FRACTION = 0.025

# -- blob -- #

BLOB_MIN_AREA = 8
BLOB_MAX_AREA = 300
MAX_BLOB_WIDTH = 45
MAX_BLOB_HEIGHT = 45

# -- wand tracker writting -- #

MIN_W = 10
MIN_H = 10

# -- interpolation -- #

INTERPOLATION_TYPE = 'linear'

# -------------- TRICK 1 -------------- #

KERNEL_FRACTION = 0.75
WITH_FIRST_FRAME = False

S_DIL_CLOAK = 20
S_ERODE_ROI = 15
S_DIL_ROI = 20

# -------------- TRICK 2 -------------- #

WAND_ITER   = 3
WAND_DILATE = 20

OBJ_ITER    = 3
OBJ_DILATE  = 20

# -------------- TRICK 3 -------------- #

TRICK3_ALPHA = 0.7              # smoothing

TRICK3_AMP_X = 1.0              # horizontal influence
TRICK3_AMP_Y_UP   = 0.5         # vertical up influence
TRICK3_AMP_Y_DOWN = 0.9         # vertical down influence 

TRICK3_MAX_DX = 20               # max horizontal wand movement per frame
TRICK3_MAX_DY = 15               # max vertical wand movement per frame

TRICK3_MAX_DY_JUMP = 15          # threshold to detect abnormal vertical jumps
TRICK3_BAD_DY_SCALE = 0.10       # strong attenuation when jump is detected

TRICK3_WAND_SUSPECT_W = 25       # suspicious wand box width
TRICK3_WAND_SUSPECT_H = 25       # suspicious wand box height
TRICK3_BAD_BOX_SCALE = 0.10      # attenuation for suspicious wand boxes

TRICK3_MASK_KERNEL = 15 
TRICK3_INPAINT_RADIUS = 9